# تعليمات المساعد البرمجي — مستودع المحتوى

> يُقرأ تلقائيًّا في كل جلسة. **قيود مطلقة لا اجتهاد فيها.**

## نطاقك

**هذا المستودع فقط: `content/`.** لا تلمس شيئًا آخر.

- ⛔ `.github/` — ملف الإشارة. تعديله يكسر النشر ويحتاج صلاحية لا تملكها.
- ⛔ مستودع القوالب `datarecovery-blog-build` — القوالب والبوابات والميزانيات.
- ⛔ مستودع الموقع `datarecovery-sa`.

سبب الحصر ليس شكليًّا: مستودع القوالب يعدّله شخص آخر بالتوازي، وأي تعديل منك
هناك يتصادم معه. أما `content/` فملفات منفصلة، كل مقال في مجلده — لا تعارض ممكن.

## بنية المقال

```
content/blog/<slug>/
    index.ar.md      ← العربية
    index.en.md      ← الإنجليزية (اختيارية)
    cover.webp       ← صورة المشاركة، داخل المجلد حصرًا
```

`<slug>`: حروف إنجليزية صغيرة وأرقام وشرطات فقط، 60 محرفًا كحد أقصى.

## الحقول — حدود يفشل البناء عند تجاوزها

```yaml
---
title: "..."                    # 10–70 محرفًا
description: "..."              # 50–160 محرفًا — يظهر في نتائج البحث
slug: "..."                     # يطابق اسم المجلد · يُقفل بعد أول نشر
date: 2026-08-15T10:00:00+03:00 # بثوانٍ ومنطقة زمنية — بدونها يفشل البناء
draft: false
categories: ["hard-drives"]     # واحد على الأقل، من القائمة أدناه
featured_image: "cover.webp"
featured_image_alt: "..."       # إلزامي وغير فارغ
---
```

**التاريخ:** بصيغة كاملة `YYYY-MM-DDTHH:mm:ss+03:00`. تاريخ بلا ثوانٍ أو بلا
منطقة زمنية يرفضه Hugo — وهذا عطل موثّق وقع فعلًا في المشروع الشقيق.

**تاريخ في المستقبل يُخفي المقال بصمت.** Hugo يستبعده بلا رسالة خطأ. استخدم
وقتًا ماضيًا.

## التصنيفات الستة — لا تخترع غيرها

| slug | العربية |
|---|---|
| `hard-drives` | الأقراص الصلبة |
| `ssd-nvme` | أقراص SSD وNVMe |
| `raid-servers` | RAID والسيرفرات |
| `ransomware` | فيروس الفدية والتشفير |
| `phones-and-cards` | الهواتف وبطاقات الذاكرة |
| `data-protection` | النسخ الاحتياطي وحماية البيانات |

مصدرها `controls/schema.json` في مستودع القوالب. تصنيف غير موجود يُفشل البناء.

## الصوت — اقرأ مقالًا قائمًا قبل أن تكتب

اقرأ `content/blog/backup-3-2-1-rule/index.ar.md` كمرجع للنبرة.

- جمل قصيرة تقريرية. هادئة، تقنية، **غير تسويقية**. بلا علامات تعجب ولا إيموجي.
- **التشخيص أولًا:** «نفحص قبل أن نحاول». لا تعد بنتيجة استعادة أبدًا.
- **حوّط كل ادعاء:** «قد»، «في كثير من الحالات»، «حسب حالة الكتابة فوقها».
- عربية سعودية رسمية بسيطة. لا فصحى تراثية ولا عامية.

## ممنوع منعًا باتًا

**لا تخترع حقائق عن العمل.** لا أسعار، ولا ضمانات، ولا نسب نجاح، ولا مدد
إنجاز، ولا شهادات، ولا جوائز، ولا أسماء عملاء، ولا آراء عملاء، ولا إحصاءات
عن حجم عملنا.

**لا تكتب «لا استعادة، لا رسوم»** — سياسة غير مؤكَّدة.

**مؤكَّد ومسموح:** «فحص وتشخيص مجاني بلا التزام» · توقيع اتفاقية عدم إفصاح عند
الطلب · **المختبر في الرياض ولا فروع في مدن أخرى**.

دراسة الحالة تُكتب كحالة توضيحية: «حالة نموذجية: …» — بلا اسم عميل ولا تاريخ
ولا نسبة دقيقة.

## قواعد تقنية

**الأرقام إنجليزية** في كل مكان حتى داخل العربية: `25` لا `٢٥`. قاعدة موقعية
مطلقة، ويوجد اختبار يمنع خرقها.

**العناوين تبدأ من مستوى 2.** عنوان المقال يصير `h1` تلقائيًّا، وعنوان مستوى 1
في المتن يجعل الصفحة بعنوانين رئيسيين ويُفشل فاحص السيو.

**الروابط الداخلية نسبية:** `/services/hdd.html` لا الرابط الكامل. المطلق يعمل
في الإنتاج ويكسر في المعاينة. والبناء يتحقق أن كل رابط يقابل صفحة موجودة.

صفحات الخدمات المتاحة للربط:
`/services/hdd.html` · `/services/ssd-nvme.html` · `/services/raid-servers.html`
`/services/cctv.html` · `/services/after-format.html` · `/services/ransomware.html`
`/services/phones.html` · `/services/memory-cards.html`

**لا HTML خام** في Markdown — معطّل في المحرّك.

**لا ترجمة آلية.** اكتب النسختين بجودة أصلية، أو انشر بالعربية وحدها.

**الصور:** WebP، أقل من 500 كيلوبايت، داخل مجلد المقال. أي صورة أكبر تُفشل البناء.

## قبل أن تسلّم

تحقق بنفسك من:

1. طول `title` و`description` ضمن الحدين
2. صفر أرقام عربية-هندية
3. `date` بصيغة كاملة وفي الماضي
4. التصنيف من الستة
5. `featured_image_alt` غير فارغ
6. كل رابط داخلي نسبي ويقابل صفحة موجودة
7. لا ادعاء غير محوّط، ولا حقيقة مخترعة عن العمل

ثم — إن توفّرت لك بيئة مستودع القوالب — شغّل:

```bash
python3 tools/prepare_content.py <مسار هذا المستودع>/content
python3 tools/check_content.py
```

## حين تشك

**توقّف واسأل.** لا تخترع حقيقة عن العمل ولا تخفّف قيدًا لتمرير بناء. البوابات
موجودة لتمسك الخطأ قبل أن يصل لعميل يبحث عن مساعدة في لحظة أزمة.
